import { IDependencies } from "@/application/interfaces/IDependencies";

export const createEnrollment = (dependencies: IDependencies) => {
    const { useCases: {createEnrollmentUseCase} } = dependencies
    
}