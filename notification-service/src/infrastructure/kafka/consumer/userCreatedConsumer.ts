// import { sendVerifcationMail } from "../../../infrastructure/services/mail/sendVerificationMail";

// export default async (data: string) => {
// 	try {
// 		await sendVerifcationMail(data);

// 		console.log("user-created-consumed mail send");
// 	} catch (error: any) {
// 		console.log("user-created-consumed mail send error: ", error?.message);
// 	}
// };
